services:
  xray:
    image: ghcr.io/xtls/xray-core:latest
    container_name: xray-bot-tproxy
    restart: unless-stopped
    network_mode: host
    command:
      - run
      - -config
      - /etc/xray/config.json
    volumes:
      - /opt/xray-bot/config.json:/etc/xray/config.json:ro
